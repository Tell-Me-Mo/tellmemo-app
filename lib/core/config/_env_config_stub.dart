// Stub implementation for non-web platforms (mobile, desktop)
// These platforms use .env file only

/// Get runtime config (not available on non-web platforms)
String? getRuntimeConfig(String key) {
  return null; // Always return null on non-web platforms
}
